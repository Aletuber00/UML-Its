#include <stdio.h>
#include <stdlib.h>
//dati N numeri interi, visualizzare il max di massimo 100 elementi e min 2.

int main () {
//definire le risorse
  
 int i = 0; //usato come indice dell aray
 int max = 0;
 int N = 0;

  do {
      printf("quanti numeri vuoi inserire? (massimo 100)");
      scanf("%d", &N);
  
}  while (N < 2 || N > 100);

   int numeri [N]; // definizione di un array di N elementi

   //inserire gli N numeri
   for (i=0; i<=N; i++) {
       printf("inserisci il %d" numero")

   }
}